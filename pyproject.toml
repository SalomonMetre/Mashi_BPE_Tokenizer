[project]
name = "bpe-tokenizer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "regex>=2026.1.15",
    "tiktoken>=0.12.0",
    "transformers>=5.1.0",
]
